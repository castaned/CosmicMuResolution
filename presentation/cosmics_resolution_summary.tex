\documentclass[aspectratio=169]{beamer}

\usetheme{Madrid}
\usecolortheme{default}
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{footline}[frame number]
\setbeamersize{text margin left=0.45cm, text margin right=0.45cm}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{tikz}
\usepackage{ifthen}

% ============================================================
% User configuration
% ============================================================
\newcommand{\Campaign}{2022F}
\newcommand{\SelectionMode}{symmetric}
\newcommand{\PeriodLabel}{Run \Campaign}
\newcommand{\DataTightDir}{DATA_tight}
\newcommand{\TightLabel}{tight}

% Edit these folder paths to point to the analysis outputs you want to present.
\newcommand{\DGLBaseDir}{../results_full_scan_DGL_2022F_symmetric}
\newcommand{\DSABaseDir}{../results_full_scan_DSA_2022F_symmetric}
\newcommand{\DGLBaseDirA}{../results_full_scan_DGL_2022F_symmetric}
\newcommand{\DGLBaseDirB}{../results_full_scan_DGL_2023D_symmetric}
\newcommand{\DGLBaseDirC}{../results_full_scan_DGL_2024I_symmetric}

% ============================================================
% Helpers
% ============================================================
\newcommand{\tightLooseTable}{
  \begin{tabular}{@{}ll@{}}
    \toprule
    Scenario & Cuts used on tag and probe \\
    \midrule
    Relaxed & no extra quality cuts beyond the upstream ntuplizer pairing \\
    Tight (DGL) & $p_{T}^{err}/p_{T} < 0.3$, $\chi^{2} < 5$, MuonHits $\geq 13$, ValidStripHits $\geq 6$ \\
    Tight (DSA) & $p_{T}^{err}/p_{T} < 0.2$, $\chi^{2} < 5$, ValidMuonDTHits $\geq 31$ \\
    \bottomrule
  \end{tabular}
}

\newcommand{\plotornote}[3][0.60\textheight]{%
  \IfFileExists{#2}{%
    \includegraphics[width=\linewidth,height=#1,keepaspectratio]{#2}%
  }{%
    \begin{tikzpicture}
      \draw[rounded corners=4pt, thick, gray!70] (0,0) rectangle (11.6,5.8);
      \node[align=center, text width=10.4cm] at (5.8,2.9) {\Large Missing plot\\[0.4em]\normalsize #3\\[0.6em]\scriptsize\ttfamily\detokenize{#2}};
    \end{tikzpicture}%
  }%
}

\newcommand{\twoplots}[4]{%
  \begin{columns}[T,onlytextwidth]
    \column{0.49\textwidth}
    \centering
    \plotornote[0.52\textheight]{#1}{#2}
    \column{0.49\textwidth}
    \centering
    \plotornote[0.52\textheight]{#3}{#4}
  \end{columns}
}

\newcommand{\singleplot}[2]{%
  \begin{center}
    \plotornote[0.60\textheight]{#1}{#2}
  \end{center}
}

\newcommand{\twoplotsplusone}[6]{%
  \twoplots{#1}{#2}{#3}{#4}
  \vspace{0.3em}
  \begin{center}
    \plotornote[0.22\textheight]{#5}{#6}
  \end{center}
}

\newcommand{\threebulletsummary}[3]{%
  \begin{itemize}
    \item #1
    \item #2
    \item #3
  \end{itemize}
}

\title{Cosmic Muon Resolution Studies}
\subtitle{DGL Results by Year and DSA Bias Status}
\author{Alfredo Casta\~neda}
\date{July 19, 2026}

\begin{document}

\begin{frame}
  \titlepage
\end{frame}

\begin{frame}{Agenda}
  \tableofcontents
\end{frame}

\section{Selection Strategy}

\begin{frame}{Goal of This Update}
  \threebulletsummary
    {Establish the DGL result as the main physics message, consistently across the 2022F, 2023D, and 2024I campaigns.}
    {Use the symmetric workflow and control plots to show that the DGL selection is stable and well behaved.}
    {Report the current DSA bias status separately, together with the upstream ntuplizer study plan.}
\end{frame}

\begin{frame}{Scenario Definitions}
  \begin{block}{Symmetric workflow}
    \tightLooseTable
  \end{block}
  \vspace{0.5em}
  \begin{itemize}
    \item The tag/probe identity still comes from the ntuplizer.
    \item In both scenarios, the listed cuts are applied symmetrically to tag and probe.
    \item For resolution studies, the probe should satisfy the same quality definition as the tag; otherwise a looser probe can bias the $p_{T}$ comparison itself.
    \item In the presentation, we report only the matched-quality resolution category.
  \end{itemize}
\end{frame}

\begin{frame}{Samples Used}
  \begin{block}{Data campaigns}
    \begin{tabular}{@{}ll@{}}
      \toprule
      Period & Input sample \\
      \midrule
      2022F & \texttt{CosmicsPPreco-CosmicDTLocalReco\_Run2022F\_Ntuples\_v3} \\
      2023D & \texttt{CosmicsPPreco-CosmicDTLocalReco\_Run2023D\_Ntuples\_v3} \\
      2024I & \texttt{CosmicsPPreco-CosmicDTLocalReco\_Run2024I\_Ntuples\_v3} \\
      \bottomrule
    \end{tabular}
  \end{block}
  \vspace{0.5em}
  \begin{block}{MC campaigns}
    \begin{tabular}{@{}ll@{}}
      \toprule
      Matched to data & GRID LFN \\
      \midrule
      2022F $\rightarrow$ 2022 & \texttt{/LooseMuCosmic\_Bin-P-10to3000-T0-Minus50to0\_cosmuogen/.../USER} \\
      2023D $\rightarrow$ 2023 & \texttt{/LooseMuCosmic\_Bin-P-10to3000-T0-Minus50to0\_cosmuogen/.../USER} \\
      2024I $\rightarrow$ 2024 & \texttt{/LooseMuCosmic\_Bin-P-10to3000-T0-Minus50to0\_cosmuogen/.../USER} \\
      \bottomrule
    \end{tabular}
  \end{block}
\end{frame}

\section{DGL Controls}

\begin{frame}{DGL Reference Category}
  \threebulletsummary
    {The DGL headline plots are based on the tight symmetric category.}
    {This category applies the same downstream quality cuts to both tag and probe and is our default for year-to-year reporting.}
    {Using only this category keeps the resolution story clean and avoids confusion from mixing looser and tighter probe definitions.}
\end{frame}

\begin{frame}{What To Check in The Control Plots}
  \threebulletsummary
    {If tag and probe show different $p_{T}^{err}/p_{T}$ spectra, first check whether their $p_{T}$ spectra are already different.}
    {The profile plot of $p_{T}^{err}/p_{T}$ versus $p_{T}$ helps separate a kinematic effect from a pure selection effect.}
    {The hit-count and $\chi^{2}$ overlays test whether the tight scenario really balances the track-quality distributions.}
\end{frame}

\begin{frame}{DGL: Tag vs Probe $p_{T}$ and $p_{T}^{err}/p_{T}$}
  \twoplots
    {\DGLBaseDir/\DataTightDir/DATA/DGL/Control_plots/pt_tag_probe_comparison.png}
    {DGL DATA tight: tag vs probe $p_{T}$}
    {\DGLBaseDir/\DataTightDir/DATA/DGL/Control_plots/ptErr_over_pt_comparison.png}
    {DGL DATA tight: tag vs probe $p_{T}^{err}/p_{T}$}
\end{frame}

\begin{frame}{DGL: Is $p_{T}^{err}/p_{T}$ Difference Mainly Kinematic?}
  \twoplots
    {\DGLBaseDir/\DataTightDir/DATA/DGL/Control_plots/tag_ptErrOverPt_vs_pt.png}
    {DGL DATA tight: tag $p_{T}^{err}/p_{T}$ vs $p_{T}$}
    {\DGLBaseDir/\DataTightDir/DATA/DGL/Control_plots/probe_ptErrOverPt_vs_pt.png}
    {DGL DATA tight: probe $p_{T}^{err}/p_{T}$ vs $p_{T}$}
\end{frame}

\begin{frame}{DGL: $p_{T}^{err}/p_{T}$ Profile Comparison}
  \singleplot
    {\DGLBaseDir/\DataTightDir/DATA/DGL/Control_plots/ptErrOverPt_vs_pt_profile_comparison.png}
    {DGL DATA tight: profile comparison}
\end{frame}

\begin{frame}{DGL: Track-Quality Control Plots}
  \twoplots
    {\DGLBaseDir/\DataTightDir/DATA/DGL/Control_plots/chi2_comparison.png}
    {DGL DATA tight: $\chi^{2}$ comparison}
    {\DGLBaseDir/\DataTightDir/DATA/DGL/Control_plots/MuonHits_comparison.png}
    {DGL DATA tight: MuonHits comparison}
\end{frame}

\begin{frame}{DGL: ValidStripHits Control Plot}
  \singleplot
    {\DGLBaseDir/\DataTightDir/DATA/DGL/Control_plots/ValidStripHits_comparison.png}
    {DGL DATA tight: ValidStripHits comparison}
\end{frame}

\section{DGL 2022F Residual Distributions}

\begin{frame}{DGL 2022F MC: Residual Distribution Comparison}
  \begin{center}
    \[
      R =
      \frac{(q/p_{T})_{\mathrm{probe}} - (q/p_{T})_{\mathrm{tag}}}
      {(q/p_{T})_{\mathrm{tag}}}
    \]
  \end{center}
  \twoplots
    {\DGLBaseDirA/MC_tight/MC/DGL/Control_plots/Tot_pthist.png}
    {DGL 2022F MC tight: standard residual}
    {\DGLBaseDirA/MC_tight/MC/DGL/pt_Plots/DGL_pt_all_fits.png}
    {DGL 2022F MC tight: residual grid vs $p_{T}$}
  \vspace{0.4em}
  \begin{itemize}
    \item These are the 2022 MC distributions matched to 2022F data; each $p_{T}$ bin contains one residual histogram, and the colored curves are fit functions over the same distribution.
    \item The single-Gaussian fit is the baseline model used to extract the reported mean and resolution; the double-Gaussian fit is kept only as a tail-shape cross-check.
  \end{itemize}
\end{frame}

\begin{frame}{DGL 2022F DATA: Residual Distribution Comparison}
  \begin{center}
    \[
      R =
      \frac{(q/p_{T})_{\mathrm{probe}} - (q/p_{T})_{\mathrm{tag}}}
      {(q/p_{T})_{\mathrm{tag}}}
    \]
  \end{center}
  \twoplots
    {\DGLBaseDirA/\DataTightDir/DATA/DGL/Control_plots/Tot_pthist.png}
    {DGL 2022F DATA tight: standard residual}
    {\DGLBaseDirA/\DataTightDir/DATA/DGL/pt_Plots/DGL_pt_all_fits.png}
    {DGL 2022F DATA tight: residual grid vs $p_{T}$}
  \vspace{0.4em}
  \begin{itemize}
    \item These are the 2022F DATA distributions; each $p_{T}$ bin contains one residual histogram, and the colored curves are fit functions over the same distribution.
    \item The single-Gaussian fit is the baseline model used to extract the reported mean and resolution; the double-Gaussian fit is kept only as a tail-shape cross-check.
  \end{itemize}
\end{frame}

\section{DGL Results by Year}

\begin{frame}{DGL 2022F: MC vs Data Mean and Sigma}
  \twoplots
    {\DGLBaseDirA/compare_MC_DATA_DGL_\TightLabel/pt_mean_mc_vs_data.png}
    {DGL 2022F tight: $p_{T}$ mean, MC vs Data}
    {\DGLBaseDirA/compare_MC_DATA_DGL_\TightLabel/pt_sigma_mc_vs_data.png}
    {DGL 2022F tight: $p_{T}$ sigma, MC vs Data}
\end{frame}

\begin{frame}{DGL 2023D: MC vs Data Mean and Sigma}
  \twoplots
    {\DGLBaseDirB/compare_MC_DATA_DGL_\TightLabel/pt_mean_mc_vs_data.png}
    {DGL 2023D tight: $p_{T}$ mean, MC vs Data}
    {\DGLBaseDirB/compare_MC_DATA_DGL_\TightLabel/pt_sigma_mc_vs_data.png}
    {DGL 2023D tight: $p_{T}$ sigma, MC vs Data}
\end{frame}

\begin{frame}{DGL 2024I: MC vs Data Mean and Sigma}
  \twoplots
    {\DGLBaseDirC/compare_MC_DATA_DGL_\TightLabel/pt_mean_mc_vs_data.png}
    {DGL 2024I tight: $p_{T}$ mean, MC vs Data}
    {\DGLBaseDirC/compare_MC_DATA_DGL_\TightLabel/pt_sigma_mc_vs_data.png}
    {DGL 2024I tight: $p_{T}$ sigma, MC vs Data}
\end{frame}

\begin{frame}{DGL Cross-Year Message}
  \threebulletsummary
    {The main reporting target is a stable DGL result across 2022F, 2023D, and 2024I.}
    {The same tight symmetric category should be used for all three years to keep the interpretation clean.}
    {Once the remaining year outputs are filled, this section becomes the main result block of the presentation.}
\end{frame}

\section{DSA Status}

\begin{frame}{Why DSA Is Separate}
  \threebulletsummary
    {The DSA study is not yet a headline resolution result because the tag/probe bias remains visible after the symmetric downstream cuts.}
    {The bias persists even for exactly-two-DSA events after matching the tag and probe quality definition.}
    {For that reason, DSA should be presented as a status and next-step study rather than as a final resolution measurement.}
\end{frame}

\begin{frame}{DSA Status: MC vs Data Mean and Sigma}
  \twoplots
    {\DSABaseDir/compare_MC_DATA_DSA_\TightLabel/pt_mean_mc_vs_data.png}
    {DSA tight: $p_{T}$ mean, MC vs Data}
    {\DSABaseDir/compare_MC_DATA_DSA_\TightLabel/pt_sigma_mc_vs_data.png}
    {DSA tight: $p_{T}$ sigma, MC vs Data}
\end{frame}

\begin{frame}{DSA MC: Residual Distribution Comparison}
  \begin{center}
    \[
      R =
      \frac{(q/p_{T})_{\mathrm{probe}} - (q/p_{T})_{\mathrm{tag}}}
      {(q/p_{T})_{\mathrm{tag}}}
    \]
  \end{center}
  \twoplots
    {\DSABaseDir/MC_tight/MC/DSA/Control_plots/Tot_pthist.png}
    {DSA MC tight: standard residual}
    {\DSABaseDir/MC_tight/MC/DSA/pt_Plots/DSA_pt_all_fits.png}
    {DSA MC tight: residual grid vs $p_{T}$}
  \vspace{0.4em}
  \begin{itemize}
    \item These histograms show directly that the residual shape is shifted rather than centered near zero.
    \item The same single-Gaussian baseline is used for the quoted DSA mean and sigma points, with the double-Gaussian fit kept only as a tail-shape cross-check.
  \end{itemize}
\end{frame}

\begin{frame}{DSA DATA: Residual Distribution Comparison}
  \begin{center}
    \[
      R =
      \frac{(q/p_{T})_{\mathrm{probe}} - (q/p_{T})_{\mathrm{tag}}}
      {(q/p_{T})_{\mathrm{tag}}}
    \]
  \end{center}
  \twoplots
    {\DSABaseDir/\DataTightDir/DATA/DSA/Control_plots/Tot_pthist.png}
    {DSA DATA tight: standard residual}
    {\DSABaseDir/\DataTightDir/DATA/DSA/pt_Plots/DSA_pt_all_fits.png}
    {DSA DATA tight: residual grid vs $p_{T}$}
  \vspace{0.4em}
  \begin{itemize}
    \item The same displaced residual shape appears in data, which is why the DSA mean and sigma summaries stay biased.
    \item This is the motivation for the control plots that follow and for the upstream ntuplizer study.
  \end{itemize}
\end{frame}

\begin{frame}{DSA Control Plots: Tag vs Probe Kinematics}
  \twoplots
    {\DSABaseDir/\DataTightDir/DATA/DSA/Control_plots/pt_tag_probe_comparison.png}
    {DSA DATA tight: tag vs probe $p_{T}$}
    {\DSABaseDir/\DataTightDir/DATA/DSA/Control_plots/ptErr_over_pt_comparison.png}
    {DSA DATA tight: tag vs probe $p_{T}^{err}/p_{T}$}
\end{frame}

\begin{frame}{DSA Control Plots: Tag vs Probe Quality}
  \twoplots
    {\DSABaseDir/\DataTightDir/DATA/DSA/Control_plots/chi2_comparison.png}
    {DSA DATA tight: $\chi^{2}$ comparison}
    {\DSABaseDir/\DataTightDir/DATA/DSA/Control_plots/ValidMuonDTHits_comparison.png}
    {DSA DATA tight: ValidMuonDTHits comparison}
\end{frame}

\begin{frame}{DSA Control Plots: Additional Hit Information}
  \singleplot
    {\DSABaseDir/\DataTightDir/DATA/DSA/Control_plots/ValidStripHits_comparison.png}
    {DSA DATA tight: ValidStripHits comparison}
\end{frame}

\begin{frame}{DSA Evidence: Tag-Probe $p_{T}$ Asymmetry}
  \twoplots
    {\DSABaseDir/\DataTightDir/DATA/DSA/Control_plots/hist_pt_ratio_probe_over_tag.png}
    {DSA DATA tight: probe $p_{T}$/tag $p_{T}$}
    {\DSABaseDir/\DataTightDir/DATA/DSA/Control_plots/hist_pt_asymmetry_probe_tag.png}
    {DSA DATA tight: $p_{T}$ asymmetry}
\end{frame}

\begin{frame}{DSA Evidence: Parameterization Cross-Checks}
  \twoplots
    {\DSABaseDir/\DataTightDir/DATA/DSA/Control_plots/probe_vs_tag_pt.png}
    {DSA DATA tight: probe $p_{T}$ vs tag $p_{T}$}
    {\DSABaseDir/\DataTightDir/DATA/DSA/Control_plots/hist_delta_abs_q_over_pt.png}
    {DSA DATA tight: probe $|q|/p_{T}$ minus tag $|q|/p_{T}$}
\end{frame}

\begin{frame}{DSA Evidence: Not Driven By $n_{\mathrm{Reco}} > 2$}
  \twoplots
    {\DSABaseDir/\DataTightDir/DATA/DSA/Control_plots/pt_ratio_probe_over_tag_ndsa_split_comparison.png}
    {DSA DATA tight: probe/tag $p_{T}$ split by multiplicity}
    {\DSABaseDir/\DataTightDir/DATA/DSA/Control_plots/pt_asymmetry_probe_tag_ndsa_split_comparison.png}
    {DSA DATA tight: $p_{T}$ asymmetry split by multiplicity}
\end{frame}

\begin{frame}{DSA Findings So Far}
  \begin{itemize}
    \item The probe tends to have systematically larger $p_{T}$ than the tag in DSA, and this directly explains the residual bias.
    \item The dedicated control plots show the same trend in both the raw $p_{T}$ comparison and in the derived $|q|/p_{T}$ difference, which points to the track-parameter definition itself rather than only to the residual formula.
    \item The effect remains when restricting to exactly two reconstructed DSA muons, so it is not mainly a multi-candidate probe-choice artifact.
    \item The next investigation therefore moves upstream, into the ntuplizer and the standalone-track quantities themselves.
  \end{itemize}
\end{frame}

\begin{frame}{Approved DSA Next Steps}
  \begin{itemize}
    \item Keep the current tag/probe selection unchanged while debugging the upstream source of the bias.
    \item Use the dedicated ntuplizer study branch with extra DSA raw branches: \texttt{p}, \texttt{qoverp}, \texttt{qoverpError}, reference point, and side label.
    \item Study the unordered exactly-two-DSA pair summary before applying any tag/probe interpretation.
    \item Decide only after that whether the issue is mainly track parameterization, detector-side asymmetry, or something else upstream.
  \end{itemize}
\end{frame}

\section{Takeaways}

\begin{frame}{Summary}
  \begin{itemize}
    \item The immediate reporting target is a clean DGL result for 2022F, 2023D, and 2024I using one consistent selection category.
    \item The DGL control plots and MC vs Data overlays form the main physics message of the current update.
    \item DSA remains an active bias study, not yet a final resolution result.
    \item The upstream ntuplizer extension is the approved next step for isolating the DSA source.
  \end{itemize}
\end{frame}

\appendix
\section{Backup}

\begin{frame}{Backup: 2024I Validation Checklist}
  \begin{itemize}
    \item In 2024I, the DGL $\sigma(p_{T})$ comparison shows MC above data in part of the range, unlike 2022F and 2023D.
    \item First check whether the 2024I MC residual histograms are broader than data in the same $p_{T}$ bins where the graph separates.
    \item Then compare 2024I MC and data control plots for $p_{T}$, $p_{T}^{err}/p_{T}$, $\chi^{2}$, MuonHits, and ValidStripHits.
    \item If the effect is concentrated only at high $p_{T}$, evaluate the role of limited statistics and residual-tail sensitivity in the fitted width.
  \end{itemize}
\end{frame}

\begin{frame}{Backup: DGL 2022F Double-Gaussian Summary}
  \twoplots
    {\DGLBaseDirA/compare_MC_DATA_DGL_\TightLabel/pt_mean_double_mc_vs_data.png}
    {DGL 2022F tight: double-Gaussian mean, MC vs Data}
    {\DGLBaseDirA/compare_MC_DATA_DGL_\TightLabel/pt_sigma_eff_double_mc_vs_data.png}
    {DGL 2022F tight: double-Gaussian $\sigma_{\mathrm{eff}}$, MC vs Data}
\end{frame}

\begin{frame}{Backup: DGL 2023D Double-Gaussian Summary}
  \twoplots
    {\DGLBaseDirB/compare_MC_DATA_DGL_\TightLabel/pt_mean_double_mc_vs_data.png}
    {DGL 2023D tight: double-Gaussian mean, MC vs Data}
    {\DGLBaseDirB/compare_MC_DATA_DGL_\TightLabel/pt_sigma_eff_double_mc_vs_data.png}
    {DGL 2023D tight: double-Gaussian $\sigma_{\mathrm{eff}}$, MC vs Data}
\end{frame}

\begin{frame}{Backup: DGL 2024I Double-Gaussian Summary}
  \twoplots
    {\DGLBaseDirC/compare_MC_DATA_DGL_\TightLabel/pt_mean_double_mc_vs_data.png}
    {DGL 2024I tight: double-Gaussian mean, MC vs Data}
    {\DGLBaseDirC/compare_MC_DATA_DGL_\TightLabel/pt_sigma_eff_double_mc_vs_data.png}
    {DGL 2024I tight: double-Gaussian $\sigma_{\mathrm{eff}}$, MC vs Data}
\end{frame}

\begin{frame}{Backup: DGL 2022F Relaxed MC vs Data}
  \twoplots
    {\DGLBaseDirA/compare_MC_DATA_DGL_relaxed/pt_mean_mc_vs_data.png}
    {DGL 2022F relaxed: $p_{T}$ mean, MC vs Data}
    {\DGLBaseDirA/compare_MC_DATA_DGL_relaxed/pt_sigma_mc_vs_data.png}
    {DGL 2022F relaxed: $p_{T}$ sigma, MC vs Data}
\end{frame}

\begin{frame}{Backup: DGL 2022F Relaxed vs Tight (Data)}
  \twoplots
    {\DGLBaseDirA/compare_scenarios_DATA_DGL/pt_mean_scenarios.png}
    {DGL 2022F data: relaxed vs tight mean}
    {\DGLBaseDirA/compare_scenarios_DATA_DGL/pt_sigma_scenarios.png}
    {DGL 2022F data: relaxed vs tight sigma}
\end{frame}

\end{document}
