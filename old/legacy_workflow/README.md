# Legacy Workflow Archive

This folder contains the older ROOT-macro-based workflow and its top-level plot outputs:

- `DGM_sel.C`
- `plot_dGlmu.py`
- `compare_mc_data_pt.py`
- legacy top-level PNG comparison plots

These files are kept for reference only. The maintained implementation lives at the repository root in `dgm_sel_rdf.py` and the associated comparison scripts.
